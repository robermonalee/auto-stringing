# AWS Deployment Requirements
# Core optimization dependencies only - no visualization libraries

# Core Python packages
numpy>=1.21.0
pandas>=1.3.0

# JSON and data handling (built-in, but listed for clarity)
# json - built-in
# csv - built-in
# re - built-in
# collections - built-in
# typing - built-in
# dataclasses - built-in (Python 3.7+)
# time - built-in
# math - built-in
# signal - built-in

# Note: matplotlib and other visualization libraries are excluded
# Use visualization_lite.py for lightweight visualization without heavy dependencies
# For full visualization, install matplotlib separately: pip install matplotlib
